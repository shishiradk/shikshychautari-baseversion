# Trimmed dependency set — only what app.py / utils.py / api.py actually import.
# Used by BOTH GitHub and the Hugging Face Space.
# The full original list (with torch/transformers/etc. for future features)
# is preserved in requirements-full.txt.

# --- Web UI / API ---
streamlit>=1.28.0
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# --- LLM / RAG ---
openai>=1.0.0
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-core>=0.1.0
langchain-community>=0.0.10
tiktoken>=0.5.0
faiss-cpu>=1.7.0

# --- PDF in / out ---
PyPDF2>=3.0.0
reportlab>=4.0.0

# --- Cloud / config ---
boto3>=1.26.0
python-dotenv>=1.0.0
